let s = "Hello World";
s.length;
s.toUpperCase();// Convert to uppercase
s.toLowerCase(); // Convert to lowercase
s.charAt(0); // Get character at index 0
s.includes("World");// Check if string contains "World"
s.replace("Hello", "Hi");// Replace "Hello" with "Hi"
s.indexOf("World"); // Get index of "World"
s.substring(0, 5);// Get substring from index 0 to 5
s.split(" "); // Split into array